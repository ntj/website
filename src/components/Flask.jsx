import React from 'react';
import './Header.css'
import Tachyons from 'tachyons/css/tachyons.min.css'

export default function Flask(){

    return (    
        <div>
            Flask is a light-weight framework to build web applications with. ORM Mappers for relational databases as well as MongoDB exist. Similar to Django,
            since it's easy to combine with React and bootstrap the fronend options are very strong.
        </div>
    )
}